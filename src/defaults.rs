use crate::screen::screen_data::{Pixel, ScreenData};

impl Default for Pixel {
  fn default() -> Self {
    Self::new()
  }
}

impl Default for ScreenData {
  fn default() -> Self {
    Self::new()
  }
}
